package org.example.uiController;

/**
 * The commands that are accepted by the system.
 */
public enum Command {
    RUN,
    LIST,
    POLICY_CHANGE,
    HELP,
    EXIT,
    UNKNOWN,
    BATCH_JOB
}
